window.APP_CONFIG = {
  // Set to true for local/offline demo using ./sample-sheet.csv
  useSampleData: true,

  // Branding
  brandName: "Expert Cabinet",
  accentColor: "#5f8bff",
  backgroundColor: "#070b14",
  cardColor: "#0f1626",

  // Course selection
  courseId: "course_alpha",

  // Public published CSV URL from Google Sheets (File -> Share -> Publish to web -> CSV)
  googleSheetCsvUrl: "https://docs.google.com/spreadsheets/d/YOUR_SHEET_ID/export?format=csv",

  // Local sample fallback (do not change unless you move the file)
  sampleCsvPath: "./sample-sheet.csv"
};
